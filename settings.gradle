pluginManagement {
    repositories {
        gradlePluginPortal()
        maven { url = 'https://maven.minecraftforge.net/' }
    }
}

rootProject.name = 'Modding'

include ':FCL'
project(':FCL').projectDir = new File(settingsDir, '../FCL-1.18')
